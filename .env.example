# Copy to `.env` and adjust. The app ships with mocks enabled so it runs
# standalone until the backend gateway is available.

# Base URL of the backend gateway (no trailing slash).
VITE_API_BASE_URL=http://localhost:8000

# Set to "false" to hit the real backend instead of the mock service layer.
VITE_USE_MOCKS=true
